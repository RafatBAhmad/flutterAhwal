name: ahwal_app
description: تطبيق يعرض حالة الحواجز العسكرية في الضفة الغربية
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_map: ^6.1.0
  vibration: ^3.1.3
  latlong2: ^0.9.0
  cupertino_icons: ^1.0.2
  http: ^1.2.0
  fluttertoast: ^8.2.4
  intl: ^0.20.2
  flutter_local_notifications: ^17.0.0

  shared_preferences: ^2.2.2
  flutter_background_service: ^5.1.0
  flutter_background_service_android: ^6.3.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true

flutter_localization:
  assets-path: l10n
  output-class: AppLocalizations
  output-dir: lib/generated
#  assets:
   # - assets/images/
    # لاحقاً تقدر تضيف صور داخل هذا المسار وتستخدمها داخل التطبيق

    #  fonts:
  #   - family: Cairo
    #   fonts:
    #   - asset: assets/fonts/Cairo.ttf
     #     weight: 700
